import { eventType } from "../eventtype"
import { blogType } from "../blogType"
import { secType } from "../secType"
import { uniType } from "../uniType"

export const schema = {
  types: [eventType, blogType, secType, uniType],
}
